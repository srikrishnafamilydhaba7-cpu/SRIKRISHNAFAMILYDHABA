# Behavioral Constraints

- **Do NOT deploy to Vercel automatically.** Only verify project correctness locally using `npm run build` or verification checks. Never run Vercel deployments unless the user explicitly requests one.

- **Do NOT commit or push to Git repository.** Keep all modifications purely local. Do not run any git commands (git add, git commit, git push, etc.) until the user explicitly requests you to update git.
